# Backend Dependencies for AiTranscribe
# ======================================

# Web framework
fastapi>=0.104.0
uvicorn>=0.24.0       # ASGI server to run FastAPI

# PyTorch (for NeMo models)
torch
torchaudio

# NVIDIA NeMo (ASR framework for Parakeet models)
nemo_toolkit[asr]

# Whisper transcription handled by whisper-cli binary (whisper.cpp with Metal GPU)
# No Python whisper dependency needed — uses subprocess

# Audio handling
sounddevice           # Record from microphone
scipy                 # WAV file I/O

# Utilities
numpy
psutil                # Memory monitoring
omegaconf             # Config management (NeMo dependency)
requests              # HTTP downloads for models
huggingface_hub       # Model cache management
setproctitle          # Set process name in Activity Monitor
